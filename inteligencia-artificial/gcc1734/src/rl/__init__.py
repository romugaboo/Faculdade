"""
Reinforcement learning utilities and training scripts for the gcc1734 project.

This package is laid out with a `src/` root; importing it directly works once the
project is installed (e.g. `pip install -e .`) or when `src` is present on the
Python path.
"""

